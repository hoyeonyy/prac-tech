server:
  port: 80
spring:
  datasource:
    url: jdbc:mysql://{RDS 주소}:3306/
    username: admin
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
